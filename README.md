# SIX9 Studio Lab

This repository is intentionally isolated from the current SIX9 production project.

## Production safety

- `main` is a protected release baseline.
- Active MVP work belongs on `mvp/locked-portal-system`.
- Do not connect any current `69design.com.au` production alias without explicit release approval.
- Do not commit production credentials or external-provider secrets.

The deployable frontend foundation is maintained on the MVP branch until production approval.
